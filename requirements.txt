streamlit # Make sure this is the latest stable version of Streamlit
requests  # To make HTTP requests to the Azure OpenAI API
feedparser  # To fetch and parse RSS feeds
openai    # For Azure OpenAI client (ensure compatibility with Azure setup)
azure-identity # Azure Identity for credential management (if needed)
