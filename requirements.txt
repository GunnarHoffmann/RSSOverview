streamlit==1.25.0  # Make sure this is the latest stable version of Streamlit
requests==2.31.0   # To make HTTP requests to the Azure OpenAI API
feedparser==6.0.10 # To fetch and parse RSS feeds
openai==0.27.0     # For Azure OpenAI client (ensure compatibility with Azure setup)
azure-identity==1.12.0  # Azure Identity for credential management (if needed)
